# Data Preparation

## FakeClue
Required inputs:
- test split JSON file
- image root directory

## LOKI
Required inputs:
- annotation JSON directory
- media root directory
- optional video frame count override

The evaluation scripts expect all dataset paths to be provided explicitly as environment variables.
